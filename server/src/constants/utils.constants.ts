// Constantes de los eventos que pueden ocurrir en el Servidor.
export const SERVER_EVENTS = Object.freeze({
  ERROR: "error",
});
// Constantes donde puede validarse un Schema.
export const DEEP_WHERE_VALIDATE_SCHEMA = Object.freeze({
  params: "params",
  body: "body",
});
